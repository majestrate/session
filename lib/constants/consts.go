package constants

/// TTL is the number of seconds that we want to store messages for by default
const TTL = 60 * 60 * 24 * 14

/// interval in seconds to refresh the snode list
const SNodeMapUpdateInterval = 60 * 2
